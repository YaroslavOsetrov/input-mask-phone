import { InputMaskPhoneDirective } from './input-mask-phone.directive';

describe('InputMaskPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaskPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
